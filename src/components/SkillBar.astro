---
const {title, value} = Astro.props;
---

<div class="flex flex-col space-y-2">
    <h5 class="font-light">{title ?? "title"}</h5>
    <div class="w-full bg-gray-200 h-2 rounded-md">
        <div class="h-2 bg-pink pl-2 text-purple text-xs rounded-md" style=`width:${value}%;`></div>
    </div>
</div>