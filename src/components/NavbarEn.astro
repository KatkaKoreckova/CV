---

---

<nav class="z-50  relative h-10">
    <div class="w-full py-5 bg-white shadow-md" id="navbar">
        <div class="content flex flex-row justify-between items-stretch">
            <!-- Logo -->
            <div class="self-center">
                <a href={`${import.meta.env.BASE_URL}/en`} class="outline-none p-0 w-24 font-bold text-2xl title" aria-label="navigate to home a">
                    K<span class="text-pink">K</span>
                </a>
            </div>
            <!-- Menu Items -->
            <div class="hidden space-x-6 self-center text-sm font-normal md:flex">
                <a href={`${import.meta.env.BASE_URL}/en/#about`} class="animated-underline self-center title" aria-label="navigate to services button">
                    About me
                </a>
                <a href={`${import.meta.env.BASE_URL}/en/#projects`} class="animated-underline self-center title" aria-label="navigate to about me button">
                    Projects
                </a>
                <a href={`${import.meta.env.BASE_URL}/en/#contact`} class="animated-underline self-center title" aria-label="navigate to contact button">
                    Contact
                </a>
                <a href={`${import.meta.env.BASE_URL}`} class="animated-underline self-center title" aria-label="navigate to SK version button">
                    SK
                </a>
            </div>

            <!-- Hamburger Icon -->
            <button id="menu-btn" class="block hamburger self-center md:hidden focus:outline-none" aria-label="toggle mobile menu button">
                <span class="hamburger-top"></span>
                <span class="hamburger-middle"></span>
                <span class="hamburger-bottom"></span>
            </button>
        </div>
    </div>
</nav>

<div id="menu" class="full-screen hidden z-50 fixed backdrop-blur-sm mt-8">
    <div class="font-semibold tracking-widest bg-white">
        <div class="flex flex-col max-w-[90%] mx-auto">
            <button id="about-btn" class="w-[95%] mx-auto outline-none border-0 text-right px-2 py-4 title" aria-label="navigate to about me button">About me</button>
            <hr class="w-full border-0 border-b border-gray-500">
            <button id="projects-btn" class="w-[95%] mx-auto outline-none border-0 text-right px-2 py-4 title" aria-label="navigate to projects button">Projects</button>
            <hr class="w-full border-0 border-b border-gray-500">
            <button id="contact-btn" class="w-[95%] mx-auto outline-none border-0 text-right px-2 py-4 title" aria-label="navigate to contact button">Contact</button>
            <hr class="w-full border-0 border-b border-gray-500">
            <a href={`${import.meta.env.BASE_URL}`} class="w-[95%] mx-auto outline-none border-0 text-right px-2 py-4 title" aria-label="navigate to SK version button">SK</a>
        </div>
    </div>
</div>